# Normalize text files to LF to keep cross-platform diffs predictable.
* text=auto eol=lf

# Windows-native scripts should keep CRLF for compatibility.
*.bat text eol=crlf
*.cmd text eol=crlf
*.ps1 text eol=crlf
