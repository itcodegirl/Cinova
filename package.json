{
  "name": "cinova",
  "private": true,
  "version": "1.0.0",
  "description": "Cinova static TMDB browser checks",
  "scripts": {
    "check": "node scripts/check.js",
    "lint": "npm run check"
  }
}
